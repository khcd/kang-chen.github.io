---
// src/pages/projects/processing-sketches.astro
import Layout from "../../layouts/Layout.astro"

const project = {
	title: "Processing Sketches",
	year: 2024,
	tags: ["Processing", "Generative Art", "Creative Coding"],
}
---

<Layout>
	<div class="project-detail">
		<h1>{project.title}</h1>
		<p class="project-meta">{project.year} · {project.tags.join(" · ")}</p>

		<div class="project-content">
			<p>
				Processing has been my sketchbook for computational ideas since I first
				encountered it. These sketches represent ongoing experiments—some
				deliberate explorations, others happy accidents discovered while
				testing parameters.
			</p>

			<p>
				The collection includes studies in colour bar motion, where vertical
				strips animate with Brownian motion patterns. There's something
				meditative about watching colour fields drift and bounce within
				constraints—ordered chaos that never quite repeats.
			</p>

			<p>
				A separate thread explores network density visualizations for Bittensor,
				attempting to represent the topology of decentralized systems in ways
				that reveal structure without overwhelming with complexity. These sketches
				iterate through different approaches to the same fundamental question:
				how do you make the invisible visible?
			</p>

			<p>
				Other experiments deal with square distortion, interpolation studies,
				and jellyfish-like particle animations. Each sketch starts small—a
				single idea tested until it either fails or opens up new directions.
			</p>
		</div>

		<div class="project-media">
			<div class="media-section">
				<h3>Meta Variations</h3>
				<div class="media-grid">
					<img src="https://media.hyperbland.com/still/meta-still-1080.jpg" alt="Meta Still" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/meta-variation.png" alt="Meta Variation" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/meta-variations1.png" alt="Meta Variations 1" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/meta-variations2.png" alt="Meta Variations 2" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/meta-variations3.png" alt="Meta Variations 3" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/meta-variations4.png" alt="Meta Variations 4" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/meta-variations5.png" alt="Meta Variations 5" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/meta-variations6.png" alt="Meta Variations 6" class="clickable-image" />
				</div>
			</div>

			<div class="media-section">
				<h3>Terrain Studies</h3>
				<div class="media-grid">
					<img src="https://media.hyperbland.com/still/terrain-1-still.jpg" alt="Terrain Study 1" class="clickable-image" />
					<img src="https://media.hyperbland.com/still/terrain-2-still.jpg" alt="Terrain Study 2" class="clickable-image" />
				</div>
			</div>
		</div>

		<div class="project-links">
			<p class="note">Collection includes 19+ discrete experiments spanning 2024.</p>
		</div>
	</div>

	<!-- Image Modal -->
	<div id="imageModal" class="modal">
		<span class="modal-close">&times;</span>
		<img class="modal-content" id="modalImage" alt="">
	</div>
</Layout>

<script>
	const modal = document.getElementById('imageModal');
	const modalImg = document.getElementById('modalImage');
	const closeBtn = document.querySelector('.modal-close');
	const clickableImages = document.querySelectorAll('.clickable-image');

	// Open modal when clicking on an image
	clickableImages.forEach(img => {
		img.addEventListener('click', function() {
			modal.style.display = 'flex';
			modalImg.src = this.src;
			modalImg.alt = this.alt;
		});
	});

	// Close modal when clicking the X
	closeBtn.addEventListener('click', function() {
		modal.style.display = 'none';
	});

	// Close modal when clicking outside the image
	modal.addEventListener('click', function(e) {
		if (e.target === modal) {
			modal.style.display = 'none';
		}
	});

	// Close modal with Escape key
	document.addEventListener('keydown', function(e) {
		if (e.key === 'Escape' && modal.style.display === 'flex') {
			modal.style.display = 'none';
		}
	});
</script>

<style>
	.project-detail {
		max-width: 800px;
		margin: 0 auto;
		padding: 3rem 1.5rem;
	}

	h1 {
		font-size: 2rem;
		font-weight: 500;
		margin-bottom: 0.5rem;
		color: var(--text-color);
	}

	.project-meta {
		font-size: 0.9rem;
		color: var(--secondary-text, #666);
		margin-bottom: 2.5rem;
	}

	.project-content {
		line-height: 1.8;
		color: var(--text-color);
	}

	.project-content p {
		margin-bottom: 1.5rem;
	}

	.project-links {
		margin-top: 2rem;
		padding-top: 1.5rem;
		border-top: 1px solid var(--border-color, #e0e0e0);
	}

	.note {
		font-size: 0.9rem;
		color: var(--secondary-text, #888);
		font-style: italic;
	}

	.project-media {
		margin-top: 3rem;
	}

	.project-media h2 {
		font-size: 1.5rem;
		font-weight: 500;
		margin-bottom: 2rem;
		color: var(--text-color);
	}

	.media-section {
		margin-bottom: 3rem;
	}

	.media-section h3 {
		font-size: 1.1rem;
		font-weight: 500;
		margin-bottom: 1rem;
		color: var(--secondary-text, #666);
	}

	.media-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
		gap: 1.5rem;
		margin-bottom: 2rem;
	}

	.media-grid img {
		width: 100%;
		border-radius: 8px;
		background-color: #000;
	}

	.clickable-image {
		cursor: pointer;
		transition: opacity 0.2s, transform 0.2s;
	}

	.clickable-image:hover {
		opacity: 0.9;
		transform: scale(1.02);
	}

	/* Modal styles */
	.modal {
		display: none;
		position: fixed;
		z-index: 1000;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		background-color: rgba(0, 0, 0, 0.95);
		align-items: center;
		justify-content: center;
		padding: 2rem;
	}

	.modal-content {
		max-width: 90%;
		max-height: 90%;
		object-fit: contain;
		border-radius: 8px;
	}

	.modal-close {
		position: absolute;
		top: 2rem;
		right: 3rem;
		color: #fff;
		font-size: 3rem;
		font-weight: 300;
		cursor: pointer;
		transition: color 0.2s;
		line-height: 1;
	}

	.modal-close:hover {
		color: #ccc;
	}

	@media (max-width: 768px) {
		.media-grid {
			grid-template-columns: 1fr;
		}

		.modal-close {
			top: 1rem;
			right: 1.5rem;
			font-size: 2.5rem;
		}

		.modal {
			padding: 1rem;
		}
	}
</style>
